# Football API key (if needed in future)
API_FOOTBALL_KEY=your-api-key-here

# Flask secret key - MUST be changed in production
SECRET_KEY=your-secret-key-here

# Flask debug mode - set to true only in development
FLASK_DEBUG=false

# OpenAI API Configuration (optional - app works without it)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key-here

# OpenAI Model Configuration (optional - uses defaults if not set)
# OPENAI_MODEL=gpt-4o-mini  # Cost-effective model (default)
# OPENAI_MAX_TOKENS=220     # Max tokens for response (default)
# OPENAI_TEMPERATURE=0.8    # Randomness level 0-2 (default)
# OPENAI_TIMEOUT=30         # API timeout in seconds (default)
