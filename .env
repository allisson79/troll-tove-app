# WARNING: Never commit real secrets to version control!
# This file is tracked for convenience, but in production:
# 1. Use real secrets (not these placeholder values)
# 2. Consider using a secrets manager or environment-specific configs

# Football API key (currently unused, for future features)
API_FOOTBALL_KEY=your-api-key-here

# Flask secret key - MUST be changed in production to a strong random value
# Generate one with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-this-to-a-random-secret-key-in-production

# Flask debug mode - set to true only in local development
FLASK_DEBUG=false


